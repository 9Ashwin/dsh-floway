# Codex reads this file; DSH ignores it and uses the SKILL.md frontmatter instead.
interface:
  display_name: "ListenHub TTS"
  short_description: "Turn text into speech through the ListenHub API"
# Human-only tool: keep it out of the model's implicit-invocation catalog.
policy:
  allow_implicit_invocation: false
